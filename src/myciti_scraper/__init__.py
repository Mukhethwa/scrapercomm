"""MyCiTi: Cape Town's bus rapid transit, as a third operator.

Golden Arrow prints scanned grids that have to be reconstructed character by character,
and PRASA's sheets are images that need OCR. MyCiTi publishes one clean PDF per route with
a real table in it, so this package is the smallest of the three by a wide margin: fetch,
read the table, load it into the same schema the other two use.
"""
